/*Create a function called greeting that takes a name as a parameter and prints 
"Hello, [name]!" in the console. */

const greeting = (name) => {
    console.log(`Hello, ${name}`);
}

greeting("Igor")