//Create a name variable with a string, an age variable with a number, and an active variable with a boolean. Print them to the console.

const name = "Aemond"
let age = 15
const active = false

console.log(name + age + active)